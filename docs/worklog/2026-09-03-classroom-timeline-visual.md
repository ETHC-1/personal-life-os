# 空教室时间轴视觉重构

- 日期：2026-09-03
- 状态：已完成

## 目标

- 顶部使用 07:00–23:00 连续时间轴，不再在顶部显示课时编号。
- 教室行内使用课时边界虚线，每两个课时之间的分隔线使用更浅颜色。
- 底部增加课时编号与时间标头，并在教室名称旁显示楼层。
- 占用区间使用蓝色到 `#5C307D` 的连续渐变进度条，移除表格式占用展示。

## 实现

- 主 Web 页面和独立设备页面统一使用时间轴布局。
- 东教学楼 1–4 教室标为一层，之后每五间递增一层。
- 页面主题覆盖为紫白配色，并更新 CSS 缓存版本。

## 验证

- 30 项 Python 单元测试通过。
- `app.js` 和 `empty-rooms.js` JavaScript 语法检查通过。
- `git diff --check` 通过。

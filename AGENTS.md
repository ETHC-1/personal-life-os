# personal-life-os 开发说明

## 项目目标

这是一个个人生活管理系统，包含日程、待办、课程同步、AI 助手、语音交互、移动端通知以及多端同步。

## 开发约定

- 开发前先阅读相关代码和 README。
- 保持模块边界清晰，避免无必要地引入大型依赖。
- 所有时间使用带时区的 ISO 8601 格式。
- API Key、密码、Cookie、课表账号和个人数据不得写入代码或提交到 Git。
- 所有外部输入都必须进行校验。
- AI 不得直接操作数据库，必须通过后端定义并校验的工具或服务完成操作。
- 日程和提醒相关功能需要考虑重复规则、时区和冲突检测。

## Git 约定

提交信息使用以下前缀：

- `feat:` 新功能
- `fix:` 修复问题
- `refactor:` 重构
- `docs:` 文档
- `chore:` 工程配置

示例：

```text
docs: add project documentation
```

## 验证要求

- 修改后运行相关测试。
- 涉及前端时检查构建是否成功。
- 涉及后端时检查接口启动和错误处理。
- 推送前确认没有敏感信息和无关的大文件。


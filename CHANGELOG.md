# 变更日志

本文件记录用户可感知的功能、体验和行为变化。

## [未发布]

- 新增 Windows 桌面端 MVP：使用 Tauri 2 封装现有 Web 前端，桌面进程启动本机 Python 服务并在退出时回收服务进程。
- 新增 `desktop/` 桌面工程、最小权限 capabilities、开发启动脚本和 Windows 桌面端说明。
- 为 Windows 桌面端补充 Tauri 所需的应用图标资源，`cargo check` 已可通过。
- 重构桌面端 UI：新增蓝白视觉系统、自定义标题栏、窗口控制、无边框圆角窗口和未来功能导航入口。
- 桌面端改为加载独立 `desktop/ui` 页面，拥有总览、接下来、今日待办、本周课程和未来能力入口，不再直接复用 Web 首页布局。
- 新增基础重复日程和提醒计算：支持每天、每周、工作日重复，以及按提前分钟数查询提醒时间。
- 新增日程和待办编辑：支持表单回填、修改后保存，日程编辑继续执行时区和冲突校验，待办编辑保留完成状态。
- 新增今日总览：聚合系统时间、今日日程和今日到期待办，首页实时显示当前时间与未完成待办数量。
- 新增待办事项功能：支持创建带优先级和截止时间的待办，在首页查看、完成/恢复及删除。
- 日历功能接入 Web 首页：支持从表单创建带时区的日程、查看未来 7 天安排并删除日程。
- 新增独立日历后端：支持带时区的事件创建、范围查询、删除、原子 JSON 存储及时间冲突校验。
- 新增 `/api/time` 系统时间接口和 `/api/calendar` 日历接口；日历文件可通过 `PERSONAL_LIFE_OS_CALENDAR_PATH` 或 `--calendar-store` 配置。
- 新增河北医科大学课表浏览器导入：打开可见浏览器，由用户手动完成 CAS 登录、密码和验证码后抓取课表。
- 新增课表文件导入：支持 JSON 和 HTML。
- 新增统一的最终课表 JSON 存储，并通过原子写入避免文件损坏。
- 新增课程导入命令行入口 `course-import`。
- 课表同步不保存账号、密码、验证码或 Cookie；浏览器抓取仅使用当前临时会话。
- 新增零依赖基础 Web 前端，支持今日概览、周课表、课程库和提醒预览；无课表时自动显示演示数据。
- 新增 `personal-life-os-web` 启动命令及 `/api/health`、`/api/courses` 接口。
- 前端新增课表导入面板，支持上传 JSON/HTML/HTM 和启动河北医科大学可见浏览器抓取流程。
- 新增 `/api/import/file` 与 `/api/import/hebmu` 导入接口，并限制上传文件类型和大小。
- 修复浏览器导入接口异常时返回 HTML 错误页的问题，前端现在会显示可读的 JSON 错误提示。
- 河北医科大学浏览器导入入口改为校园网首页 `https://jwweb.hebmu.edu.cn/`，不再依赖 CAS 统一认证入口。
- 新增独立 `course-import empty-room` 命令，可抓取指定楼栋和日期的教室使用记录，暂不与课表前端合并。
- 合并校园网课程与教室使用抓取流程，新增一次登录连续调用两类接口的 Web/CLI 入口。
- 前端新增东教学楼 1—19 教室的 13 课时可视化时间轴，显示具体时间并区分空闲/有课状态。
- 优化空教室视图为“教室纵轴、时间横轴”，并使用贯穿矩阵的竖向虚线标记各课时。
- 空教室查询暂限定为中山校区东教学楼，楼栋信息固定为东教学楼，查询日期默认今天和明天。
- 空教室时间轴改为 06:00—24:00 的均匀小时刻度；隐藏课时编号，使用虚线标记课时边界，并以橙红渐变条显示占用时段、留白显示空闲时段。
- 修复空教室接口响应嵌套在 `data.jsylist` 或 `data` 数组时未被解析的问题；前端同时兼容教室名称中的空白差异，并在接口未返回占用课时时提示检查响应。
- 修复校园网空教室接口实际字段为 `jxllist` 时被误判为空的问题。
- 增强空教室响应解析：兼容多层 `data/result/rows` 包装和字符串化 JSON，并仅选择含教室名称字段的记录。
- 兼容空教室响应中可能出现的 `jsyzlist` 列表字段及未知名称的嵌套列表。
- 按实际校园网响应补充 `jszylist` 字段解析；支持从 `jxcdmc` 和 `jcdm2` 生成教室占用时段。
- 当空教室接口仅返回 `code/msg` 而非列表时，前端显示接口消息，后端日志仅记录非敏感状态信息。
- 浏览器导入会话监听页面自身请求并在内存中复用 `token` 请求头，课程和空教室请求共享该临时认证状态。
- 合并导入支持临时粘贴微信授权回调链接，并校验仅允许河北医科大学 `/app/` HTTPS 链接；授权链接不落盘。
- 将空教室抓取拆为独立 `empty-room-poll` worker，支持持久化浏览器 Profile、一次性抓取和每天 06:00—21:00 每小时轮询，不改动课表抓取流程。
- 新增本地 `empty-room-bridge` 和 Fiddler Classic 转发脚本：从微信沙箱成功响应中提取脱敏空教室数据，可选上传到云端。
- 本地采集桥新增状态页 `http://127.0.0.1:8765/`，可查看最近一次采集是否成功。
- Fiddler 转发脚本在本机转发前剥离姓名、课程和认证相关字段，仅保留教室、课时与日期。
- 验证本地微信采集桥成功接收并解析真实空教室响应。
- 浏览器会话登录后主动调用 `getIstoken` 获取接口 token，再请求课表和空教室数据。
- 接入本地 Fiddler 空教室桥快照：Web 前端新增 `/api/empty-rooms`，启动和手动刷新时自动读取脱敏结果，课表接口保持不变。
- 新增 `empty-room-direct-probe` 安全诊断命令：通过临时环境变量测试空教室 API 是否能脱离微信页面直接调用，不输出或保存凭证。
- 直接 API 探针补充 `Origin`、`Referer` 和 `User-Agent` 复现能力，并将全量 Fiddler 导出文件加入 Git 忽略。
- 优化空教室时间轴：相邻占用课时合并为连续渐变条，鼠标悬停时显示完整占用时间范围。
- 新增 Ubuntu 部署说明及 systemd Web/空教室定时服务模板。
- 在 README 中补充 LLM/Agent 目标、脚本与 Agent 分工、受控工具边界及实施阶段。
- 新增 `empty-room-direct` 直接 API 抓取命令，支持今天/明天并写入脱敏快照；Web 接口兼容该快照格式。

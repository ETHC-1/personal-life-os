[package]
name = "personal-life-os-desktop"
version = "0.1.0"
description = "Windows desktop shell for personal-life-os"
authors = ["personal-life-os"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[features]
custom-protocol = ["tauri/custom-protocol"]
